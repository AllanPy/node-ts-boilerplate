class EntityService {
    static async getAllEntities(arg1: any, arg2: any): Promise<any> {
        // const results = await axiosConfig.request({
        //     method: 'GET',
        //     url: `description=${description}&location=${location}&page=${page}&search=${search}&markdown=true`,
        //     });
        //     await cache.saveObject('', results.data);
        return [];
    }
    async getEntityById(arg1: any, arg2: any) {}
    async createEntity(arg1: any, arg2: any) {}
    async updateEntity(arg1: any, arg2: any) {}
}

export default EntityService;
